<div class="panel panel-default">
  <div class="panel-body activity">  
    <% if activity.trackable.present? %>
      <%= link_to activity.trackable.name, user_url(activity.trackable) %> just joined!
    <% else %>
      Someone just joined! (since removed)
    <% end %>
  </div>
</div>
