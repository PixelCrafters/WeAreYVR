<div class="col-sm-10 no-buffer">
  <%= form_for @profile_link, method: :post, html: {class: "form-inline", role: "form"} do |f| %>
    <div class="form-group">
      <%= f.text_field :name, class: "form-control small-field", placeholder: "name" %>
    </div>
    <div class="form-group">
      <%= f.text_field :url, class: "form-control small-field", placeholder: "url" %>
    </div>
    <%= f.hidden_field :linkable_id, :value => @organization.id %>
    <%= f.hidden_field :linkable_type, :value => "Organization" %>
    <%= f.submit "Add Link", class: "btn btn-sm btn-default" %>
  <% end %>
</div>
